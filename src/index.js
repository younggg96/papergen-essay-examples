import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import EssayExamplesPage from './pages/EssayExamplesPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <EssayExamplesPage />
  </React.StrictMode>
);
